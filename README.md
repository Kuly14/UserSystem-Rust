# Access system written in RUST

This software allows you to create a profile with username and password. It then hashes the password and stores it to data.txt

Then you can log in with your credentials.

